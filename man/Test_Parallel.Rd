% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Test_Permutation.R
\name{Test_Parallel}
\alias{Test_Parallel}
\title{Parallel Testing of Record Process Models}
\usage{
Test_Parallel(
  X,
  obs_type = c("all", "records"),
  record_times = NA,
  alpha = 0.05,
  lag = 10,
  warmup = 2,
  approximate = FALSE,
  one.sided = FALSE,
  method = "Bonf",
  K = NULL,
  estimate_gamma = TRUE,
  gamma = NULL,
  RSq = 0.8
)
}
\arguments{
\item{X}{Numeric vector. The observed series.}

\item{obs_type}{String. "all" or "records" depending on the series provided.}

\item{record_times}{(Default = NA) record times in case obs_type = "records"}

\item{alpha}{Numeric. Significance level for all tests (default = 0.05).}

\item{lag}{Integer. Lag parameter for Box-Jenkins test (default = 10).}

\item{warmup}{Integer. Warm-up parameter for YNM test (default = 2).}

\item{approximate}{Logical, if \code{TRUE} use the asymptotic normal approximation
(default = \code{FALSE} for the exact quantile test).}

\item{one.sided}{Logical, if \code{TRUE} perform a one-sided test
(default = \code{FALSE} for two-sided).}

\item{method}{Character, p-value combination method: one of
\code{"Bonf"}, \code{"Holm"}, \code{"Sidak"}, or \code{"Chisq"} (default = "Bonf").}

\item{K}{Optional. Number of partitions in Test_YNM_Pearson test.
If given, force exactly K partitions using quantiles.}

\item{estimate_gamma}{Logical. If to estimate \eqn{\gamma} through minimizing \eqn{\chi^2} (Default = TRUE)}

\item{gamma}{Numeric. Optional. Force if estimated_gamma = FALSE.}

\item{RSq}{Numeric, minimum adjusted R-squared required to accept the LDM
hypothesis (default = 0.8).}
}
\value{
A list containing all:
\describe{
\item{results}{List of all Test outcomes}
\item{decision}{Final decision of each test}
}
}
\description{
Applies all 10 possible Tests (if applicable) of four record-based tests (D, L, C, Y)
on the same series \code{X} independently.
}
\examples{
tests = Test_Parallel(X=rnorm(50), obs_type = "all", alpha = 0.05)
tests$decision
# iid_NT.decision        DTRW_NT.decision
# "Classical"                  "DTRW"
# YNM_NT.decision       YNM_Pearson.decision
#           "NO"                      NA
# YNM_Geom.decision         LDM_NT.decision
#            "NO"                    "NO"
# LDM_Sequential.decision     LDM_Regression.decision
#     "NO"                           "NO"
# iid_Box.decision     DTRW_Indep.decision
#  "Classical"             "NO"
}
