% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rec_count_stats.R
\name{rec_count_mean_LDM}
\alias{rec_count_mean_LDM}
\title{Expected number of records in Linear Drift Model (LDM)}
\usage{
rec_count_mean_LDM(
  T,
  theta,
  dist = c("beta", "gumbel", "weibull", "frechet", "norm", "exp", "pareto", "uniform"),
  n_sim = 1000,
  ...
)
}
\arguments{
\item{T}{Integer. The length of the series.}

\item{theta}{Numeric. The linear drift coefficient \eqn{\theta > 0}.}

\item{dist}{Character, distribution name. One of:
"beta", "gumbel", "weibull", "frechet", "exp", "pareto", "norm", "exp", "pareto", "uniform".}

\item{n_sim}{Numeric. Number of Monte Carlo simulations (default 1000)}

\item{...}{Additional parameters specific to the chosen distribution:
\describe{
\item{beta}{\code{shape1}, \code{shape2}}
\item{gumbel}{\code{loc}, \code{scale}}
\item{weibull}{\code{shape}, \code{scale}}
\item{frechet}{\code{shape}, \code{scale}}
\item{norm}{\code{loc}, \code{sd}}
\item{exp}{\code{rate}}
\item{pareto}{\code{scale}, \code{shape}}
\item{unifom}{\code{min}, \code{max}}
}}
}
\value{
a single value of the expected number of records
}
\description{
The average expected number of records in a LDM process with a Gumbel underlying distribution.
}
\details{
An LDM process is defined as \eqn{X_t = Y_t + \theta t },
where \eqn{Y_t} are independent and identically distributed random variable.

An explicit formula exists when \eqn{Y_t} follow a \eqn{Gumbel(\alpha, \beta)} distribution. It is computed as:

\deqn{E(N_T) = \sum_{t=1}^{T} \frac{1-e^{\theta / \beta}}{1-e^{\theta t / \beta}}}

For other increment distributions, the variance is estimated by simulation.
}
\examples{
rec_count_mean_LDM(T=25, theta=0.5, dist="gumbel", n_sim=100,location=0, scale=1)
# [1] 10.93343

rec_count_mean_LDM(T=25, theta=0.5, dist="gumbel", n_sim=100,location=0, scale=2)
# [1] 7.320699

rec_count_mean_LDM(T=25, theta=0.5, dist="norm", n_sim=100,mean=0, sd=1)
# [1] 13.18
}
