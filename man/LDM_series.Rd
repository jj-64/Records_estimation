% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Generate_series.R
\name{LDM_series}
\alias{LDM_series}
\title{Generate a Linear Drift Model (LDM) Series with different Noise distribution}
\usage{
LDM_series(
  T,
  theta,
  dist = c("beta", "gumbel", "weibull", "frechet", "norm", "exp", "pareto", "uniform"),
  ...
)
}
\arguments{
\item{T}{Integer. The length of the series.}

\item{theta}{Numeric. The linear drift coefficient \eqn{\theta > 0}.}

\item{dist}{Character, distribution name. One of:
"beta", "gumbel", "weibull", "frechet", "exp", "pareto", "norm", "exp",
"pareto", "uniform".}

\item{...}{Additional parameters specific to the chosen distribution:
\describe{
\item{beta}{\code{shape1}, \code{shape2}}
\item{gumbel}{\code{loc}, \code{scale}}
\item{weibull}{\code{shape}, \code{scale}}
\item{frechet}{\code{shape}, \code{scale}}
\item{norm}{\code{mean}, \code{sd}}
\item{exp}{\code{rate}}
\item{pareto}{\code{scale}, \code{shape}}
\item{unifom}{\code{min}, \code{max}}
}}
}
\value{
A numeric vector representing the LDM series.
}
\description{
Generates an LDM series where the noise follows a user defined distribution.
}
\details{
The series is generated as:
\deqn{x_t = \theta t + y_t, \quad y_t \sim \text{F}(\text{.}, \beta)}
where:
\itemize{
\item \eqn{\theta} is the linear drift.
\item \eqn{y_t} are i.i.d. distributed random variables.
\item \eqn{\beta} are specific parameters of the distribution F of \eqn{Y} .
}
}
\examples{
#Gumbel with loc, scale
LDM_series(100, theta = 0.1, dist = "gumbel", loc = 0, scale = 2)

#Weibull
LDM_series(100, theta = 0.05, dist = "weibull", shape = 2, scale = 1)

#Beta
LDM_series(100, theta = 0.2, dist = "beta", shape1 = 2, shape2 = 5)

#Normal
LDM_series(100, theta = 0.1, dist = "norm", mean = 0, sd = 1)
}
