% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/describe_records.R
\name{describe_records}
\alias{describe_records}
\title{Describe a vector and append record-based statistics}
\usage{
describe_records(x, na.rm = TRUE, attach_details = FALSE)
}
\arguments{
\item{x}{A vector accepted by \code{\link[psych:describe]{psych::describe()}}. For now, this function
expects a single vector (not a data frame or matrix). Supply one column
at a time if needed.}

\item{na.rm}{Logical; remove \code{NA}s before computing both the \code{describe} and
the record-based statistics. Default \code{TRUE}.}

\item{attach_details}{Logical; if \code{TRUE}, the full vectors returned by
\code{rec_values()} and \code{rec_times()} are attached as an attribute
\code{record_details} to the returned data frame. Default \code{FALSE}.}
}
\value{
A data frame identical to \code{\link[psych:describe]{psych::describe()}} but with the columns
listed above appended. When \code{attach_details = TRUE}, an attribute
\code{record_details} is attached.
}
\description{
A thin wrapper around \code{\link[psych:describe]{psych::describe()}} that appends record-based
statistics computed from \code{rec_count()}, \code{rec_values()}, and \code{rec_times()}.
The return value preserves the structure of \code{psych::describe()} (a data frame)
so it can be used as a drop-in replacement in pipelines.
}
\details{
Extra columns appended to the \code{psych::describe()} output:
\itemize{
\item \code{obs_rec_count}: number of records from \code{rec_count(x)}.
\item \code{obs_rate}: records per observation, i.e., \code{obs_rec_count / length(x)} after \code{na.rm}.
\item \code{first_rec_value}, \code{last_rec_value}: first/last elements of \code{rec_values(x)}.
\item \code{mean_rec_value}, \code{min_rec_value}, \code{max_rec_value}: summaries of \code{rec_values(x)} (numeric only; otherwise \code{NA}).
\item \code{first_rec_time}, \code{last_rec_time}: first/last elements of \code{rec_times(x)}.
\item \code{rec_span_secs}: if times are \code{POSIXt} or \code{Date}, the span (last - first) in seconds; otherwise \code{NA}.
}

When \code{attach_details = TRUE}, the full vectors are added as an attribute
\code{record_details = list(values = <...>, times = <...>)} so you can inspect
or reuse them without bloating the data frame with list-columns.
}
\examples{
\dontrun{
  set.seed(1)
  x <- rnorm(100)
  x[sample.int(100, 10)] <- NA

  out <- describe_records(x)
  out

  # With the full record vectors attached
  out2 <- describe_records(x, attach_details = TRUE)
  attr(out2, "record_details")
}

}
\seealso{
\code{\link[psych:describe]{psych::describe()}}
}
